---
description: 下单贴轨：按工程流程对指定逐秒分析报告执行贴轨
---

执行贴轨任务，目标报告：$ARGUMENTS

**一律按 `SOP_每集贴轨全流程.md`（2026-09-10 实测版）执行**，要点：

1. 工程搭建：`D:\reaper工程\` 下新建本工程文件夹，复制模板 `C:\Users\Administrator\Desktop\reaper工程模板\模板.rpp` 为 `<工程名>.rpp` 再打开（模板原件只读）；**参考视频插入轨 0（Video）@0.0**，长度以 ffprobe 为准（视频路径由制作人提供）。
2. 环境就绪：REAPER 启动/激活（2309→2308）+ reapy_boost 连接自检 + 模板轨序实测（见 SOP 轨序表）。
3. 拆 cue：按报告文本与 §2.5 口径（【自动】【魔法】配，【手动】【对白】【待定】跳过）；制作人若有免检口径，三处留痕，免检不免三重自检。
4. 执行：**主对话单层直派「见习音效师」**（派活单自包含：路径、轨序表、BASE、口径、铁律、落盘要求；硬条款=音源必查资产表并引用记录、脚本必抄 place 模板、禁止猜文件名）。音效总监负责拆解口径把关与事后复核。
5. 三重自检（对账/重叠/越界）全过 → `Main_SaveProjectEx` 保存 + ISDIRTY 清零 → REAPER 保持打开。
6. 落盘三件套到 cues/（选材与cue清单、place 脚本、交班报告，命名含集号+日期）。

上游报告只读；不启动第二个 REAPER 实例。
