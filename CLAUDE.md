# 见习音效师工程（Claude Code 版）· 工作守则

这是一个影视音效（foley）贴轨工程工作区。凡在本工作区内工作，主对话 agent 必须遵循以下架构与纪律。

> 本工程是 ZCode 版（`..\见习音效师工程\`，已冻结）的 Claude Code 移植版。方法论权威：`Desktop\音效工作流交接包_20260907\`。

## 角色分工

- **用户（制作人）**：下达任务、最终拍板。
- **主对话 agent（制作助理）**：理解需求、拆解任务、用 Agent 工具按角色派活、汇总结果，不亲自执行分析或贴轨细节。
- **视频分析师**（`.claude/agents/视频分析师.md`）：按 `video-action-analysis` skill 的 SOP 对视频做逐秒动作分析，产出贴轨 cue 报告到**本次任务的具体工作目录**（`reports/`，落盘规则见「文件流转约定」）。
- **音效总监**（`.claude/agents/音效总监.md`）：接单拆解任务、给见习音效师派活、复核交付、出具验收结论。只复核不亲自贴轨。
- **见习音效师**（`.claude/agents/见习音效师.md`）：按 `reaper-foley-workflow` skill 的 SOP 执行具体贴轨，产出交班报告。

派活顺序：制作人 → 主对话 → 视频分析师（出报告）→ 音效总监（拆解/复核）→ 见习音效师（执行）。

## 文件流转约定

| 目录 | 内容 | 规则 |
|---|---|---|
| `sources/` | 原始视频素材 | 只读 |
| `cues/` | 选材记录、贴轨 cue 清单、自检与验收报告 | 见习音效师/总监产出，可追溯 |
| `sessions/` | REAPER 工程（.rpp） | 每集一个 Region 工程 |
| `exports/` | 分轨导出成品 | 命名含集号与日期 |

所有中间产物必须落盘，禁止只在对话里口头交付。

**产物落盘位置（2026-09-20 制作人条款）**：抽帧工作区（frames）与逐秒分析报告（reports）**不落本 agent 工程**——一律落到本次任务的具体工作目录（由制作人开工时指定，如 `D:\reaper工程\<工程名>_<集号>_<日期>\` 下建 `reports/` 与 `frames/`）。报告交付后视为只读，禁止修改判定。未指定工作目录时先问制作人，禁止默认写进本工程目录。

## 纪律

- 上游报告可疑（疑似假动作、时间轴异常）时上报制作人，任何人不得改判。
- 每次交付必须附三重自检结论；未通过就是未通过，禁止美化。
- 快捷入口：`/分析 <视频路径>` 出报告，`/贴轨 <报告路径>` 下单，`/质检 <交付路径>` 验收；新用户（或换机/配置缺失）先 `/开始` 配置音源库。
- 涉及 REAPER 实机操作一律走 `reaper-foley-workflow` / `reaper-remote-ops` skill 中的实测命令，不自创 API 用法。
- **音画条款与数值纪律（20260916 制作人验收条款，长期有效）**：
  - **音量一律不动**：foley 条目 D_VOL 保持默认 1.0，禁止任何调量（环境垫/动效也不例外）。
  - **脚步声一律不配**：脚步节奏无法与画面对齐，脚步（含怪物足步/爬行步）归人工环节，AI 不选材不贴轨。
  - **start_offset 默认为 0**：一律从源头部起播；非零取窗必须先出 ffmpeg volumedetect 证据（窗口内 max_volume > −30dB），禁止盲选。
  - **杂集素材默认不作候选**：>10s 的杂集/compilation 类素材（如 creature_foley_ck 40s 集锦）默认排候选末位，确需使用须全窗有声证据并存疑清单放行。
  - **口径冲突即停即问**：见习/总监上报的口径冲突（如派单与 skill 相抵），值班必须暂停该单请示制作人，禁止按单硬贴（20260916 音量事故教训）。
  - **数值禁止手抄**：跨派单传递的工程数值（BASE/集偏移/区间/时长）必须引用落盘文件（如 offsets_*.json），禁止从对话手抄（20260916 事故：手抄 BASE 错 2.375s，69 条返工）。
- **并发限流铁律（20260914 事故）**：子 agent 并发派活 ≤2 路，默认 1 路串行——2026-09-14 夜间批次并行派 5 个分析师即触发 429/1302 限流，引擎崩溃退出、整夜任务中断。细节与重启标准动作见 `SOP_每集贴轨全流程.md`「并发与限流纪律」节。

## 标准施工流程（2026-09-10 实测跑通版）

- **每集贴轨一律走 `SOP_每集贴轨全流程.md`**：①工程搭建（模板复制 + **参考视频入轨**）→ ②环境就绪 → ③拆 cue → ④资产表选材（每条引用资产记录）→ ⑤抄定稿模板填 CUES → ⑥贴轨 → ⑦三重自检 → ⑧保存 → ⑨落盘三件套 → ⑩复核验收。
- 制作人免检口径（如跳过图片核验）必须三处留痕（脚本头注释/选材清单/交班报告），免检不免三重自检。
- 派活模式：执行层由主对话**单层直派**「见习音效师」（嵌套孵化链实测慢且不透明）；音效总监负责拆解口径与事后复核验收。

## 新任务运行约定（2026-09-10 制作人拍板）

- **历史工程冻结**：往期工程内容不再推进（ep18~25 取窗回填、V1 帧秒抽查、7~12 集旧报告复检、主工程改名、卡通音效课等旧挂账一律不启动，以实际工程推进为准）。交接包 `Desktop\音效工作流交接包_20260907\` 的方法论（十篇文档 + 三个 skill）迁移到新任务使用。
- **视频素材路径**：每次任务开工前由制作人现场提供具体位置，不预置、不猜测。
- **REAPER 工程模板**：`C:\Users\Administrator\Desktop\reaper工程模板\模板.rpp` 为只读原件。每次新建工程：先在 `D:\reaper工程\` 下新建本工程文件夹，把模板**另存**进去后再开工，严禁污染模板原件。
- **音源库（双版本，2026-09-20 起通用化）**：
  - 本机版：`音效工作流交接包_20260907\scripts\pipeline\asset_library.db`（实测 61,397 条），path 前缀 `D:\音效文件\` 本机有效（2026-09-10 核验通过，详见 `cues\音源库与资产表检查_20260910.md`）。
  - NAS 通用版：`\\192.168.9.251\音效2\资源库\_索引\asset_library_nas.db`（60,871 条，path 前缀即 NAS 资源库，其他用户零本地依赖）；工程 `assets\` 与交接包 pipeline 另有两处同 sha1 副本。生成器 `scripts\make_nas_asset_db.py` + 规则 `scripts\nas_path_map.json`，映射报告 `cues\资产表NAS版映射报告_20260920.md`（526 条 NAS 真缺失已剔除并进存疑清单）。
  - 用哪版由机器配置 `~/.claude/foley_machine_config.json` 决定（`/开始` 生成）；未配置先跑 `/开始`，禁止猜路径。检索引擎 `search_assets.py` 副本在 NAS `_索引\` 与工程 `scripts\`。选材规则照交接包 04 号文档。
- **环境自检**（REAPER Distant API / reapy_boost / ffmpeg）：挂账，随实际任务推进逐步验证，不提前空跑。`/开始` 只做存在性状态报告，不属空跑。

## 新用户首次引导（/开始，2026-09-20）

- 新用户第一次打开本工程、换机、或音源库配置缺失时，先跑 `/开始`：检测音源库模式（NAS `\\192.168.9.251\音效2\资源库` / 本地 `D:\音效文件`）→ 用户自选资产表 db 来源（NAS `_索引\` / 工程 `assets\` / 交接包 / 自定义）→ 轻量环境检测（只报状态）→ 写机器级配置 `~/.claude/foley_machine_config.json`。
- 机器配置只是**默认值来源**：每集施工时 skill 的 `foley_project.json` 仍是权威（SOP 步骤① init 引用机器配置默认值）；`/开始` 不写 foley_project.json。
- 全角色护栏：音源库路径未配置时引导跑 `/开始`，禁止猜路径。
- bash 里用 python 探测/访问 NAS（UNC 路径）必须写临时脚本文件执行，禁止 `python -c` 内联传 UNC 路径——实测内联会静默失败（2026-09-20 教训）。

## 工程架构（Claude Code）

```
见习音效师工程_claude/
├── CLAUDE.md                   ← 本文件：工程级总指令
├── SOP_每集贴轨全流程.md        ← 标准施工流程（与交接包文档冲突时以它为准）
├── .claude/
│   ├── agents/                 ← subagent 定义
│   │   ├── 视频分析师.md        上游：逐秒动作分析，产出 cue 报告
│   │   ├── 见习音效师.md        执行者：按 SOP 贴轨，产出交班报告
│   │   └── 音效总监.md          负责人：拆解派活、复核验收
│   ├── commands/               ← slash 命令
│   │   ├── 开始.md             /开始（新用户首次引导，配置音源库）
│   │   ├── 分析.md             /分析 <视频路径>
│   │   ├── 贴轨.md             /贴轨 <报告路径>
│   │   └── 质检.md             /质检 <交付路径>
│   └── skills/                 ← 工程专属技能（本工程内副本）
│       ├── reaper-foley-workflow/
│       ├── video-action-analysis/
│       └── reaper-remote-ops/
├── sources/                    原始视频素材（只读）
├── cues/                       选材/cue 清单/自检/验收产物
├── scripts/                    工程侧工具（NAS 版资产表生成器、检索引擎副本）
├── assets/                     资产表 NAS 版工程内副本（gitignore 不入库）
├── sessions/                   REAPER 工程
└── exports/                    分轨导出成品

（frames/ 与 reports/ 不设在本工程——抽帧工作区与分析报告一律落本次任务的具体工作目录，见「文件流转约定」）
```

skill 双份安装：工程内 `.claude/skills/`（随本目录复制复用）+ 全局 `~/.claude/skills/`（所有工程可用）。快照来源：`Desktop\音效工作流交接包_20260907\skill快照\`——skill 更新时以快照为源，两处同步。
