# 变更记录（本工程仓库，只记历史）

> 本文件只记「哪天改了什么」，不含规则。规则正文一律在 CLAUDE.md、SOP 与 skill。
> 2026-09-21 制作人条款：skill/脚本同步后默认提交并推送本仓库（origin: foley-agent-project），并在此记一行，不留未提交挂账。

## 2026-09-20

- 初始备份提交（05e412c）。
- NAS 通用版资产表 + 新用户首次引导 `/开始`（b14764b）。

## 2026-09-21

- **skill 同步**（源：交接包 `skill快照\`，工程内 + 全局两处安装副本同改）：
  - reaper-foley-workflow `pitfalls.md`：随公开库去个人化清理，原机检测脚本路径改为「已随旧环境废弃」。
  - video-action-analysis：抽帧默认 480p 省 token（`--scale` 默认 600→480、manifest 记 scale、SKILL「第一步」同步口径）。
- **`scripts\search_assets.py` 按交接包新版重分发**（制作人拍板「按新版来」）：
  - key 改读环境变量 `DASHSCOPE_API_KEY`（其次同目录 config.json `retrieval.api_key`），无 key 自动降级原词检索不报错；不再依赖旧私有配置文件。
  - CLI db 查找回退链覆盖四布局，本工程命中旁级 `assets\asset_library_nas.db`（实测「开门」→ door 系扩展检索正常）。
  - 修复降级失效 bug：`_api_key()` 在 try 外构造 Request，无 key 时异常穿顶崩溃（实测抓到后修复）。
  - 原机 key 已由旧私有配置迁移 `setx DASHSCOPE_API_KEY`（用户级环境变量，重启后的新会话生效）。
