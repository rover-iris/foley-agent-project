# 每集贴轨全流程 SOP（2026-09-10 实测跑通版）

> 来源：权臣CP 第001集测试单实测（12 条音频 cue + 视频入轨，三重自检全过，制作人验收「能操作跑通」）。
> 本文件是本工程的标准施工流程；与交接包文档冲突时，以本文 + 实测记录为准。
> 配套实测记录：`cues\音源库与资产表检查_20260910.md`、`cues\第001集_交班报告_20260910.md`

## 流程总链

```
接单 → ①工程搭建（复制模板 + 视频入轨）→ ②环境就绪（REAPER + reapy 连接自检）
→ ③读报告拆 cue → ④查资产表选材 → ⑤抄模板填 CUES 表 → ⑥执行贴轨
→ ⑦三重自检 → ⑧保存工程 → ⑨落盘三件套 → ⑩复核验收
```

## ① 工程搭建

- 新工程：在 `D:\reaper工程\<工程名>_<集号>_<日期>\` 新建文件夹，把 `C:\Users\Administrator\Desktop\reaper工程模板\模板.rpp` **复制**为 `<工程名>.rpp` 再打开。模板原件只读；同目录其他临时文件（`模板.rpp-NEWTEMP`、`模板_临时测试.rpp`）一律不碰。
- **视频入轨（必做项——001 集测试曾漏，已补）**：参考视频插入轨 0（Video）位置 0.0，长度 = ffprobe 实测值；贴完读回 source filename 防空块；该条目不计 CUES、不参与三重自检对账，交班报告单独补记。
- **逐集 Region 选区（20260916 新增定稿）**：视频入轨后，对轨 0 各视频条目逐集创建 Region 选区（名称=集号，区间=条目起点~终点），供制作人在标尺上跳转、逐集审听。脚本：`交接包\scripts\示例与工具\create_video_regions_首尾相连_20260916.py`（幂等可重跑，按位置对账自动补缺，建完自动保存）。来源：制作人 Lua 脚本「一键视频整理-首尾相连」阶段 2 的 Python 定稿版；连映多集工程必做，单集工程同样适用。
- 视频素材路径每次由制作人开工时提供。

## ② 环境就绪（本机实测参数）

- REAPER 7.52：`C:\Program Files\REAPER (x64)\reaper.exe`，可命令行带工程路径直接启动。
- reapy_boost 0.10.201（Python312）已装；依赖 typing_extensions 已补。
- 若 REAPER 未运行：启动后 curl 触发激活动作 `http://127.0.0.1:2309/_/_RSZuNAdyBEl5JXJYJ5Ax7AyaNgjqka1MsR99VputBI`（阻塞数十秒属正常），等 2308 LISTENING 即就绪（Web 接口 csurf 已在 REAPER.ini 配好，激活动作在 reaper-kb.ini）。
- 连接自检：脚本顶部清 HTTP_PROXY/HTTPS_PROXY/ALL_PROXY、设 NO_PROXY=127.0.0.1,localhost → `connect(Host(IPv4Address("127.0.0.1")))` → sleep(1) → `hasattr(RPR,'CountTracks')` 断言 → `GetProjectName("","",512)` 出工程名。
- 弹窗原则照 08 号文档：检测 + 报告 + 请人工点，绝不自动化点弹窗；绝不盲目开第二个 REAPER 实例。

## 模板轨序表（实测，索引自 0）

| 索引 | 轨 |
|---|---|
| 0 | Video（参考视频专用） |
| 1~5 | 对话组（室内 / 心声 / 室外 / 怪兽） |
| 6~12 | 环境fx（父轨 6，子轨 7~12 = 环境fx 01~06） |
| 13~28 | 动作fx（父轨 13；14=脚步，15=脚步(D)，16=衣物摩擦，17~28=动作fx 03~14） |
| 29~35 | 过渡fx（子轨 30~35） |
| 36~40 | BGM（子轨 37~40） |
| 41~45 | 编组 / 效果 / 混响（43=室内rv 44=室外rv 45=心声rv） |
| 46~48 | 其他效果 |
| 49 | 总输出 |

环境底贴 7~12；动效按音色贴 14~28；分轨导出只导动作组（沿交接包 06 号口径）。

## ③ 拆 cue

- 报告文本 = 施工标准；【自动】配、【魔法】按正货包自动配、【手动】不配、【对白】跳过、【待定】跳过（09 号 §2.5 口径）。
- **口径描述保持「动作开头、材质括号备注」**（20260916 制作人动作优先条款）：总监拆口径、见习写 CUES 的 label 沿用报告的事件主词（动作）为第一顺位，材质词置于括号备注——是书写次序的优先级，材质信息保留；材质被改写成主词 = 优先级颠倒（下游按主词检索，已复现「金属撞击配人动作」事故），此类条目按存疑退回。
- **制作人可下免检口径**（如「跳过图片核验按文本直贴」）——免检口径必须**原样写进脚本头注释、选材清单、交班报告三处留痕**；免检不免三重自检。
- 文本模糊处按字面直贴，并在记录里标「按文本直贴」。

## ④ 选材（资产表强制引用）

- DB（双版本，2026-09-20 起）：本机版 `Desktop\音效工作流交接包_20260907\scripts\pipeline\asset_library.db`（61,397 条，path 前缀 `D:\音效文件\`）；NAS 通用版 `\\192.168.9.251\音效2\资源库\_索引\asset_library_nas.db`（60,871 条，path 即 NAS 绝对路径；526 条 NAS 真缺失已剔除，清单见 `cues\资产表NAS版映射报告_20260920.md`）。用哪版由机器配置 `~/.claude/foley_machine_config.json`（`/开始` 生成）决定，施工时写进本工程 `foley_project.json`；未配置先跑 `/开始`，禁止猜路径。
- 检索姿势：`sys.path.insert` 含 search_assets.py 的目录（机器配置 `pipeline_dir`：本机=交接包 pipeline，NAS 用户=`资源库\_索引\`，工程内副本=`scripts\`）后 `from search_assets import search`；或 q() SQL 模式（交接包 04 号）。**CLI 直跑 search_assets.py 会报错**（index/ 硬编码坑）。长环境底加 `WHERE duration >= N` 过滤。bash 里用 python 访问 NAS（UNC 路径）必须写脚本文件执行，禁止 `python -c` 内联（实测静默失败）。
- 规则：集内同源 ≤2 次、异窗轮换、玄幻用正货包；**星级不硬卡**——同音色池内 5 星前置，音色与星级冲突时音色匹配优先并留痕（20260916 定稿）；**每条 cue 记录资产引用（name/rating/duration），禁止猜文件名、禁止 glob 摸文件系统**。
- **脚步声一律不配（20260916 制作人条款）**：脚步节奏无法与画面对齐，脚步（含怪物足步/爬行步）归人工环节；报告中的脚步类事件一律归【手动】不配，AI 不选材不贴轨。
- DB 的 cues 表（856 行）len 全空不可用；start_offset 一律由 CUES 表手工设计。

## ⑤~⑥ 贴轨执行

- 抄 `scripts\示例与工具\place_ep26_v1.py` 定稿模板，只改 BASE / S 字典 / CUES；**track_idx 必须换成上表新轨序**（模板里是老工程轨序）。禁止从零重写连接/防重/贴轨逻辑。
- D_STARTOFFS 显式写 `SetMediaItemTakeInfo_Value(take,'D_STARTOFFS',off)`；REAPER 条目名 = 源文件名 stem；**音量一律不动**：item D_VOL 保持默认 1.0，禁止 0.3~0.8 之类的调量（20260916 制作人验收条款，与 skill 2026-09-11「一律默认音量」一致；2026 夜间批次曾误沿用旧 vol 口径贴了 531 条，已于当日脚本归位）；幂等防重可安全重跑，对账不足自动第二轮。
- **start_offset 新规（20260916 制作人验收条款）**：非零 offset 默认禁止——一律从源头部起播（off=0）；确需取窗的，必须先跑 ffmpeg volumedetect 出证据（窗口内 max_volume > -30dB 才允许），**禁止盲选 offset**（20260916 实测：盲选窗口 3/4 为数字静音 -90dB，另 1 处内容错配）。
- **素材黑名单（优先级表述）**：>10s 的杂集/compilation 类素材（如 creature_foley_ck 系列 40s 集锦）**默认不作候选**，确需使用须全窗 volumedetect 有声证据并存疑清单放行——内部静音占比高、内容不可控（20260916 实测：一条 40s 杂集 75% 是静音段）。
- 长环境底可同轨相邻多条拼接（不重叠）。

## ⑦~⑧ 三重自检 + 保存

1. **区间对账**：目标区间 item 数 == CUES 条数（不信脚本计数器）。
2. **同轨重叠**：0 处。
3. **越界审计**：每条 off+len ≤ DB 源 duration，0 条。
- 保存：`Main_SaveProjectEx(0, 工程路径, 0)` + `GetSetProjectInfo(0,'PROJECT_ISDIRTY',0,True)` 清假阳性；**REAPER 保持打开**供制作人审听（除非另有指示）。

## ⑨ 落盘（cues\，命名含集号 + 日期）

- `<集号>_选材与cue清单_<日期>.md`（含资产引用逐条）
- `<集号>_place脚本_<日期>.py`（执行版副本；插视频等辅助脚本同样留档）
- `<集号>_交班报告_<日期>.md`（步骤 / 自检 / 免检留痕 / 待复核 / **存疑音色清单**：靠关键词匹配但音色无法自证的条目逐条列出，供总监抽核与制作人抽听——AI 无试听能力，音色匹配责任上移人工，20260916 条款）
- 总监验收：`验收_<集号>_<日期>.md`

## ⑩ 复核验收

- 总监复核三重自检数据自洽 + 抽查 ≥20% cue；免检单不做图片抽查，按留痕口径复核。
- 结论：通过 / 打回（问题清单）/ 上报；结论先行，落盘 `cues\`。

## 派活模式（2026-09-10 实测定稿）

- **执行层单层直派**：主对话持明确口径直接派「见习音效师」——实测比「总监再孵化见习生」的嵌套链更快更稳、过程可控；总监负责拆解口径与事后复核验收。
- 派活单必须自包含：报告 / 工程 / DB 路径、轨序表、BASE、§2.5 口径、免检口径（如有）、贴轨铁律、落盘要求；并写明硬条款：**音源必查资产表、脚本必抄定稿模板、禁止猜文件名、交班不贴脚本全文**。
- **数值禁止手抄（20260916 事故条款）**：派单中的 BASE/集偏移/区间/时长必须注明来源落盘文件（如 `offsets_*.json`）并照抄该文件值；从对话手抄数字一律禁止（20260916 夜批手抄 BASE 错 2.375s，07/08 集共 69 条返工）。
- **口径冲突即停即问（20260916 音量事故条款）**：见习/总监上报口径冲突（派单与 skill/SOP/制作人既往条款相抵）时，值班必须暂停该单、请示制作人；「按单继续」不是值班可自行裁决的事项。口径冲突与解决结果必须写进交班报告与进度文件双留痕。

## 并发与限流纪律（2026-09-14 3D妹宝夜间批次事故，实测教训）

**事故经过**：夜间批次 23:38 主对话一次性并行派出 5 个后台「视频分析师」子 agent（加主会话共 6 路并发 API 流），23:39:48 起 22 秒内连吃 15 次 `429 / 1302 账户已达到速率限制`，ZCode 重试耗尽后抛未处理 `PROVIDER_BUSINESS_ERROR` **直接崩溃退出**。夜间任务开工约 10 分钟即中断，分析报告 0 份产出（Phase 0 成果因「每步即时保存」纪律零损失）。

**规则（后续所有批次派活必须遵守）**：
1. **子 agent 并发 ≤2 路，默认 1 路串行**。批次分析、总监复核等并行派活前，先按账户限流口径收敛路数；宁可慢，不可崩。
2. 派活后若日志出现 429/1302：立即降并发、串行重放，禁止原样重试。
3. **崩溃/中断后重启的标准动作**：先盘点落盘现场（.rpp 条目数、进度.md、reports/、cues/），已保存成果直接复用，从断点续跑，不重建工程。
4. 长时段无人值守任务：保持「每集完成即保存工程+更新进度」纪律不变（本次零损失全靠它）；启动器脚本建议同时把 stderr 落盘（本次靠它才定位到根因）。



## 本机 API 实测坑（reapy_boost 0.10.201 + REAPER 7.52，001 集实测）

| 坑 | 实测结论 |
|---|---|
| RPR 函数出参必须传全 | `GetProjectName("","",512)`；`CountProjectMarkers(0,0,0)` 返回 4 元素 list |
| GetTrackName 名字在下标 [2] | [0]=retval，[1]=track 指针，[2]=名字 |
| GetTrackUIVolPan vol 在 [2] | [1] 是 track 指针（08 号文档同款坑） |
| P_NAME 读回易取到键名位 | 条目名以 .rpp 内 `NAME` 行落盘为准 |
| .rpp 文本统计注意 CRLF | 跨行正则要吃 \r\n，否则漏计数 |
| 02 号文档连接示例参数不全 | `GetProjectName("",512)` 在本机 TypeError，按本表写法 |
| CountProjectMarkers 出参不可信（20260916） | `(0,0,0)` 返回的 [1][2]（markers/regions 数）在包装层会误报 0；总条数取 [0]，Region 对账一律 EnumProjectMarkers 逐条枚举、按位置比对 |
| EnumProjectMarkers 名称出参回显占位符（20260916） | 传入的 name 占位串会被原样返回（读不到真名），对账禁止比名称；删 marker 按返回值末位 **marker id** 识别，勿按枚举序号猜（序号≠id，实测曾因此删错一条） |

## 已知气质欠账（权臣CP 001 集，换源时参考）

- 啜饮：全库无真人喝声，暂用卡通库 `BT HUMAN Mouth Water Slurp`。
- 群笑垫：含德语交谈底（`MLCK LOCATION Marketplace Small 04`）。
- 内院垫：5 星前置后鸟鸣偏弱（`QP02 0220`，备选 `QP01 0116`）。

## 20260916 制作人验收反馈（三条整改，全部已执行/已立规）

1. **脚步声不配**：制作人条款「脚步频率不可能跟画面对得上，以后脚步不用 AI 管」→ SOP ④ 已立规：脚步类（含怪物足步/爬行）归【手动】不配。存量：夜批 15 集共贴 89 条脚步类条目，待制作人决定是否移除。
2. **音量**：夜批派单误沿用旧稿「vol 环境 0.3~0.5/动效 0.5~0.8」，531 条全被调量（ep03 见习曾上报口径冲突，值班未纠正——教训：口径冲突必须停下问制作人，不得按单硬贴）。已跑 `3D七宗罪音效实验\cues\02_音量归位_20260916.py` 全部归位 1.0 并保存；SOP ⑤⑥ vol 指引已删除改为「一律不动」。
3. **裁剪位置（以 ep03 @55~58s 为证）**：动作fx4/动作fx9 上的 BARRA1/BARRA2/BODYFALL/STEPC 四条——ffmpeg 实测 off=4/9/6 三个窗口全部为数字静音（max -90~-91dB），STEPC off=26 窗口有声但内容是硬质单响（听感≈出拳）；BARRA 源是「Metal Massive Bar Rattle」重型金属条撞击（听感≈钢铁落地）；BODYFALL/STEPC 源是 creature_foley_ck **40s 杂集**（75% 静音段，0 星）。根因：关键词选材无试听环节 + start_offset 盲选（全 15 集 527 条中 421 条 off>0 均为盲选）。整改：SOP ⑤⑥ 新增 offset 默认 0 + 非零须 volumedetect 证据 + 杂集类黑名单；存量 421 条非零 offset 待制作人授权后可做全量静音审计出清单（删/归零两案待选）。
